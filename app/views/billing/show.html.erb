<div class="page-shell stack-md">
  <section class="theme-hero p-5">
    <div class="level is-align-items-flex-start">
      <div class="level-left">
        <div>
          <div class="theme-section-label">Account</div>
          <h1 class="page-title">Upgrade</h1>
        </div>
      </div>
      <div class="level-right">
        <span class="theme-chip <%= premium_access? ? 'is-live' : '' %>">Current: <%= Current.user.account_tier.humanize %></span>
      </div>
    </div>
  </section>

  <section class="columns is-multiline is-variable is-3">
    <% @tiers.each do |tier| %>
      <% current_tier = Current.user.account_tier == tier[:key] %>
      <div class="column is-12-tablet is-4-desktop">
        <article class="theme-card p-5 h-full stack-md" style="<%= current_tier ? 'border-color:var(--theme-primary); box-shadow:0 0 0 1px var(--theme-primary);' : '' %>">
          <div>
            <div class="is-flex is-justify-content-space-between is-align-items-center">
              <h2 class="section-title"><%= tier[:name] %></h2>
              <% if current_tier %><span class="theme-chip is-live">Current</span><% end %>
            </div>
            <p class="page-copy mt-2"><%= tier[:description] %></p>
          </div>

          <div>
            <span class="bebas text-5xl"><%= tier[:price] %></span>
            <span class="help-theme">/ <%= tier[:interval] %></span>
          </div>

          <ul class="stack-sm" style="list-style:none; padding:0; margin:0; flex:1">
            <% tier[:features].each do |feature| %><li class="mono text-sm">✓ <%= feature %></li><% end %>
          </ul>

          <% if current_tier %>
            <button type="button" class="button theme-btn-outline is-fullwidth mono" disabled>Current plan</button>
          <% elsif tier[:key] == "free" %>
            <button type="button" class="button theme-btn-outline is-fullwidth mono" disabled>Included</button>
          <% else %>
            <button type="button" class="button theme-btn-primary is-fullwidth mono" disabled data-stripe-price-placeholder="<%= tier[:key] %>">Upgrade with Stripe soon</button>
          <% end %>
        </article>
      </div>
    <% end %>
  </section>

  <div class="theme-blank-slate mono text-sm">Future Stripe hook: replace disabled upgrade buttons with Checkout session creation for the selected tier.</div>
</div>
