<!DOCTYPE html>
<html>
<head>
<title>DartMatch</title>
<%= csrf_meta_tags %>
<%= csp_meta_tag %>
  <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
<body class="bg-slate-900 text-slate-100 min-h-screen">
<header class="bg-slate-800 border-b border-slate-700">
<div class="max-w-5xl mx-auto px-4 py-3 flex justify-between items-center">
<h1 class="font-bold tracking-wide">🎯 DartMatch</h1>
<nav class="text-sm">
<% if Current.user %>
<span class="text-slate-300 mr-3"><%= Current.user.email_address %></span>
<%= button_to "Logout", session_path, method: :delete, class: "bg-red-600 hover:bg-red-500 px-3 py-1 rounded" %>
<% else %>
<%= link_to "Login", new_session_path, class: "underline" %>
<% end %>
</nav>
</div>
</header>


<main class="max-w-5xl mx-auto px-4 py-6">
<%= yield %>
</main>
</body>
</html>
