<div class="page-shell">
  <section class="theme-hero p-5">
    <div class="theme-section-label"><%= t("invites.expired_label") %></div>
    <h1 class="page-title"><%= t("invites.expired_title") %></h1>
    <p class="page-copy mt-2"><%= t("invites.expired_copy") %></p>
    <%= link_to t("invites.back_to_matches"), matches_path, class: "button theme-btn-primary mt-4" %>
  </section>
</div>
